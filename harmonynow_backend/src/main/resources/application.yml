spring:
  application.name: harmonynow_backend
  datasource:
    hikari:
      maximum-pool-size: 4
    url: jdbc:postgresql://localhost:5432/harmonynow
    username: admin_jhhan
    password: admin_jhhan

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

    defer-datasource-initialization: true

  thymeleaf:
    cache: false

  sql:
    init:
      platform: postgres
      data-locations: classpath*:db/data.sql
      mode: always

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: trace

file:
  path: C:/Users/hanju/harmonynow/
